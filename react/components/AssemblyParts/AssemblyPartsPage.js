import React from "react";

const AssemblyPartsPage = ({ test }) => {
  return (
    <h1>
      ASSEMBLY PARTS PAGE (ONLY ROUTABLE IF AUTHENTICATED) test prop: {test}
    </h1>
  );
};

export default AssemblyPartsPage;
